{% extends 'base.html.twig' %}

{% block body %}
    <div class="container mt-5 mb-5">
        <h1><a href="https://sarg.lt">Sarg.lt</a> reporterių skelbimų archyvas su paprasta paieška :)</h1>
        <h5>Šis puslapis naudoja client-side pagination, tai reiškia, jog užtruks ilgiau užkrauti skelbimus, tačiau pagreitės tolimesnis filtravimas.</h5>
        <h5>Saugoma {{ ads|length }} unikalių skelbimų nuo 2021-01-30</h5>
        <table id="table"
               data-toggle="table"
               data-search="true"
               data-show-columns="true"
               data-pagination="true"
               data-page-size="25"
        >
            <thead>
            <tr>
                <th>Data</th>
                <th data-visible="false">Reporteris</th>
                <th>Skelbimas</th>
            </tr>
            </thead>
            <tbody>
            {% for ad in ads %}
                <tr>
                    <td>{{ ad.datetime|date("Y-m-d H:i:s") }}</td>
                    <td>{{ ad.reporter }}</td>
                    <td>{{ ad.text }}</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}
